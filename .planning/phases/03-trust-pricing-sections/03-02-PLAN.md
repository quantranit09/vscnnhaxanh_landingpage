---
plan: 03-02-testimonials-about
phase: 3
wave: 2
depends_on: [03-01-pricing-partners]
requirements_addressed: [TEST-01, TEST-02, TEST-03, ABOUT-01, ABOUT-02, ABOUT-03]
files_modified:
  - src/sections/Testimonials.tsx
  - src/sections/About.tsx
  - src/app/page.tsx
autonomous: true
---

# Plan 03-02: Testimonials & About Section

## Objective
Xây dựng 2 sections cuối cùng trước form liên hệ: **Testimonials** (5 đánh giá khách hàng dạng thẻ masonry hoặc grid) và **About Us** (Giới thiệu ngắn gọn về công ty Nhà Xanh và các quận phục vụ tại Đà Nẵng).

## Content Design

### Testimonials
```
5 Reviews thực tế:
1. Chị Thu Hương (Quản lý VP Hải Châu) — "Đội ngũ Nhà Xanh cọ rửa văn phòng rất kỹ. Kính sáng bóng, thảm giặt xong không bị ẩm mùi. Sẽ ký hợp đồng dài hạn." (5 sao)
2. Anh Quốc Bảo (Chủ nhà, Liên Chiểu) — "Sạch sẽ, nhanh gọn. Mình mới xây nhà xong bụi kinh khủng mà gọi các bạn qua dọn 1 ngày là vào ở được luôn. Giá hợp lý." (5 sao)
3. Chị Mai Lan (Chủ shop, Thanh Khê) — "Hóa chất mùi dễ chịu, không bị hắc như mấy chỗ trước mình thuê. Điểm cộng là các bạn vệ sinh kính mặt tiền rát sạch." (4.5 sao)
4. Giám đốc Kho Vina (Ngũ Hành Sơn) — "Dọn dẹp kho bãi chuyên nghiệp, máy móc xịn. Có xuất hóa đơn VAT đầy đủ cho công ty, rất ưng ý." (5 sao)
5. Cô Tuyết (Căn hộ Sơn Trà) — "Mấy em trẻ mà làm việc có tâm lắm. Lau dọn từng ngóc ngách, bếp núc dầu mỡ sạch bóng. Rất khuyến khích mọi người dùng thử." (5 sao)
```

### About Us
Nội dung giới thiệu sự tận tâm, chuyên nghiệp của Nhà Xanh.
Mục list 7 khu vực phục vụ: Hải Châu, Thanh Khê, Sơn Trà, Ngũ Hành Sơn, Liên Chiểu, Cẩm Lệ, Hòa Vang.

## Tasks

### Task 1: Create Testimonials.tsx

<read_first>
- `src/components/ui/SectionTitle.tsx`
</read_first>

<action>
Create `src/sections/Testimonials.tsx`:

```tsx
'use client'

import { motion } from 'framer-motion'
import { Star, StarHalf } from 'lucide-react'
import { SectionTitle } from '@/components/ui'

const testimonials = [
  {
    name: 'Chị Thu Hương',
    role: 'Quản lý VP, Hải Châu',
    content: 'Đội ngũ Nhà Xanh làm việc rất kỹ. Kính văn phòng sáng bóng, thảm giặt xong không bị ẩm mùi. Sẽ ký hợp đồng dài hạn.',
    rating: 5,
    initial: 'H'
  },
  {
    name: 'Anh Quốc Bảo',
    role: 'Chủ nhà, Liên Chiểu',
    content: 'Sạch sẽ, nhanh gọn. Mình mới xây nhà xong bụi kinh khủng mà gọi các bạn qua dọn 1 ngày là vào ở được luôn. Mức giá rất hợp lý.',
    rating: 5,
    initial: 'B'
  },
  {
    name: 'Chị Mai Lan',
    role: 'Chủ shop, Thanh Khê',
    content: 'Hóa chất mùi dễ chịu, không bị hắc như mấy chỗ trước đây. Điểm cộng lớn là các bạn vệ sinh kính mặt tiền cửa hàng cực kỳ sạch.',
    rating: 4.5,
    initial: 'L'
  },
  {
    name: 'Anh Tuấn Anh',
    role: 'Quản lý Kho, Ngũ Hành Sơn',
    content: 'Dọn dẹp kho bãi công nghiệp rất chuyên nghiệp, máy móc hiện đại. Đặc biệt là có xuất hóa đơn VAT đầy đủ, nhanh chóng cho công ty.',
    rating: 5,
    initial: 'T'
  },
  {
    name: 'Cô Tuyết',
    role: 'Cư dân, Sơn Trà',
    content: 'Mấy em làm việc có tâm lắm. Lau dọn từng ngóc ngách, khu vực bếp dính nhiều dầu mỡ đều sạch bong. Lần sau cô lại gọi.',
    rating: 5,
    initial: 'T'
  },
]

function renderStars(rating: number) {
  const stars = []
  for (let i = 1; i <= 5; i++) {
    if (i <= rating) {
      stars.push(<Star key={i} size={16} className="fill-amber-400 text-amber-400" />)
    } else if (i - 0.5 === rating) {
      stars.push(<StarHalf key={i} size={16} className="fill-amber-400 text-amber-400" />)
    } else {
      stars.push(<Star key={i} size={16} className="text-gray-300" />)
    }
  }
  return <div className="flex gap-1 mb-4">{stars}</div>
}

export function Testimonials() {
  return (
    <section className="py-20 md:py-28 bg-[#F5F7F5]">
      <div className="container">
        <SectionTitle
          badge="Khách hàng nói gì?"
          title="Đánh Giá Chân Thực Về Nhà Xanh"
          subtitle="Hơn 100+ khách hàng tại Đà Nẵng đã trải nghiệm và hài lòng với chất lượng vệ sinh của chúng tôi."
        />

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 auto-rows-min">
          {testimonials.map((review, i) => (
            <motion.div
              key={i}
              className={`bg-white p-8 rounded-2xl shadow-sm border border-gray-100 ${
                i === 3 || i === 4 ? 'lg:col-span-1 md:col-span-2 lg:col-start-auto' : ''
              }`}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.1, duration: 0.5 }}
            >
              {renderStars(review.rating)}
              <p className="text-gray-700 italic mb-6 leading-relaxed">
                &ldquo;{review.content}&rdquo;
              </p>
              <div className="flex items-center gap-4">
                <div className="w-12 h-12 bg-[#2D8B3A]/10 text-[#2D8B3A] rounded-full flex items-center justify-center font-bold text-lg">
                  {review.initial}
                </div>
                <div>
                  <div className="font-bold text-gray-900">{review.name}</div>
                  <div className="text-sm text-gray-500">{review.role}</div>
                </div>
              </div>
            </motion.div>
          ))}
          
          {/* Summary Box */}
          <motion.div
            className="bg-[#2D8B3A] p-8 rounded-2xl text-white flex flex-col justify-center items-center text-center"
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            transition={{ delay: 0.5, duration: 0.5 }}
          >
            <div className="text-5xl font-black mb-2">4.9/5</div>
            <div className="flex gap-1 mb-4">
              {[1, 2, 3, 4, 5].map((i) => (
                <Star key={i} size={20} className="fill-white text-white" />
              ))}
            </div>
            <p className="text-emerald-100 font-medium">Trung bình đánh giá từ 100+ khách hàng tại Đà Nẵng</p>
          </motion.div>
        </div>
      </div>
    </section>
  )
}
```
</action>

<acceptance_criteria>
- [ ] `Testimonials.tsx` created
- [ ] Renders stars based on rating
- [ ] 5 reviews + 1 summary card
</acceptance_criteria>

### Task 2: Create About.tsx

<action>
Create `src/sections/About.tsx`:

```tsx
'use client'

import { motion } from 'framer-motion'
import { MapPin, Trophy, ShieldCheck } from 'lucide-react'

const districts = [
  'Hải Châu', 'Thanh Khê', 'Sơn Trà', 'Ngũ Hành Sơn', 
  'Liên Chiểu', 'Cẩm Lệ', 'Hòa Vang'
]

export function About() {
  return (
    <section className="py-20 md:py-28 bg-white overflow-hidden">
      <div className="container">
        <div className="grid lg:grid-cols-2 gap-16 items-center">
          {/* Left: Content */}
          <motion.div
            initial={{ opacity: 0, x: -40 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
          >
            <span className="text-[#2D8B3A] font-bold tracking-wider uppercase text-sm mb-4 block">
              Về Chúng Tôi
            </span>
            <h2 className="text-3xl md:text-4xl font-extrabold text-gray-900 mb-6 leading-tight">
              Nhà Xanh — Tiêu Chuẩn Nâng Tầm Không Gian Sống
            </h2>
            <div className="space-y-5 text-[#4A5A4A] leading-relaxed mb-8">
              <p>
                Được thành lập với sứ mệnh mang đến môi trường sống và làm việc sạch sẽ, an toàn nhất, 
                <strong> Vệ Sinh Công Nghiệp Nhà Xanh</strong> tự hào là đối tác tin cậy của hàng trăm hộ gia đình và doanh nghiệp tại Đà Nẵng.
              </p>
              <p>
                Chúng tôi không chỉ làm sạch bề mặt, chúng tôi dọn dẹp vi khuẩn, nấm mốc và các tác nhân gây hại bằng hệ thống máy móc nhập khẩu nguyên chiếc và 100% hóa chất đạt chứng nhận an toàn, thân thiện với môi trường.
              </p>
            </div>

            <div className="flex flex-wrap gap-6 mb-10">
              <div className="flex items-center gap-3">
                <div className="w-12 h-12 bg-orange-100 text-orange-600 rounded-full flex items-center justify-center">
                  <Trophy size={20} />
                </div>
                <div className="font-bold text-gray-900">Dịch vụ Top 1<br/><span className="text-sm font-normal text-gray-500">Đà Nẵng & Hội An</span></div>
              </div>
              <div className="flex items-center gap-3">
                <div className="w-12 h-12 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center">
                  <ShieldCheck size={20} />
                </div>
                <div className="font-bold text-gray-900">Chứng nhận an toàn<br/><span className="text-sm font-normal text-gray-500">Hóa chất Châu Âu</span></div>
              </div>
            </div>
            
            <hr className="border-gray-100 mb-8" />
            
            <h3 className="font-bold border-l-4 border-[#2D8B3A] pl-3 mb-4 text-gray-900">
              Khu vực phục vụ (Đội xe 24/7)
            </h3>
            <div className="flex flex-wrap gap-2">
              {districts.map(d => (
                <span key={d} className="inline-flex items-center gap-1.5 bg-gray-50 px-3 py-1.5 rounded-md text-sm text-gray-600">
                  <MapPin size={14} className="text-[#2D8B3A]" />
                  Quận {d}
                </span>
              ))}
            </div>
          </motion.div>

          {/* Right: Mock Image Panel */}
          <motion.div
            initial={{ opacity: 0, x: 40 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6, delay: 0.2 }}
            className="relative"
          >
            <div className="aspect-[4/3] rounded-3xl overflow-hidden bg-gray-100 relative">
              {/* Replace with real image in production */}
              <div className="absolute inset-0 bg-[#E8F3E9] flex flex-col items-center justify-center p-8 text-center border-4 border-white shadow-inner">
                <div className="w-24 h-24 bg-[#2D8B3A] rounded-2xl flex items-center justify-center rotate-12 mb-6">
                  <span className="text-white font-black text-4xl -rotate-12">NX</span>
                </div>
                <p className="text-[#2D8B3A] font-bold text-xl mb-2">Đội ngũ chuyên nghiệp</p>
                <p className="text-gray-500 text-sm max-w-xs">Hình ảnh thực tế đội ngũ thi công Nhà Xanh sẽ được cập nhật tại đây.</p>
              </div>
            </div>
            
            {/* Floating badge */}
            <div className="absolute -bottom-6 -left-6 bg-white p-6 rounded-2xl shadow-xl border border-gray-100">
              <div className="flex items-center gap-4">
                <div className="text-4xl font-black text-[#2D8B3A]">30+</div>
                <div className="text-sm font-semibold text-gray-700 leading-tight">
                  Chuyên viên <br/>vệ sinh lành nghề
                </div>
              </div>
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  )
}
```
</action>

<acceptance_criteria>
- [ ] `About.tsx` created
- [ ] Mentions 7 districts of Da Nang
- [ ] Includes placeholder for real team image
</acceptance_criteria>

### Task 3: Add to page.tsx and index.ts

<action>
Export `Testimonials` and `About` in `src/sections/index.ts`.
Add `<Testimonials />` and `<About />` to `src/app/page.tsx` after Pricing.
Run `npm run build` to verify.
</action>

<acceptance_criteria>
- [ ] Build succeeds
</acceptance_criteria>
