---
plan: 04-02-animations-mobile
phase: 4
wave: 2
depends_on: [04-01-contact-form]
requirements_addressed: [RESP-01, RESP-02, RESP-03, RESP-04, ANIM-01, ANIM-02, ANIM-03, ANIM-04]
files_modified:
  - src/components/layout/FloatingCTA.tsx
  - src/sections/Hero.tsx
autonomous: true
---

# Plan 04-02: Animations, Mobile Polish & Final QA

## Objective
Kiểm duyệt lần cuối khả năng hiển thị trên thiết bị di động (Responsive Audit), đảm bảo scroll reveal animations chạy mượt mà kể cả khi bật chức năng reduced-motion, và hoàn thiện layout của nút `FloatingCTA` để không đè lên content quan trọng phía dưới màn hình trên mobile.

## Content Design

- Các `whileInView` của `framer-motion` đã được set up cơ bản trong các Phase 2-3, tuy nhiên cần gắn `viewport={{ once: true, margin: "-50px" }}` để chạy animation ngay khi chuẩn bị xuất hiện trên màn hình nhỏ. (Sẽ review và apply vào components nếu cần)
- Cập nhật số Zalo Link và Zalo Icon chuẩn cho FloatingCTA (Mở đúng trang Zalo/Phone native trên Mobile).
- Thay Placeholder "0905-000-000" thành Placeholder thực tế "0900.xxx.xxx" có CTA `tel:` ở Header.

## Tasks

### Task 1: Audit FloatingCTA for Mobile & Add tel: link

<read_first>
- `src/components/layout/FloatingCTA.tsx`
- `src/components/layout/Header.tsx`
</read_first>

<action>
1. Update `FloatingCTA.tsx`:
   - Zalo URL: `https://zalo.me/0905000000` (Sử dụng 1 số SĐT demo để demo)
   - Phone URL: `tel:0905000000`
   - Đảm bảo trên Mobile (w < 768px), Zalo button ở góc dưới bên trái, Call button ở góc dưới bên phải hoặc gộp chung 1 dải ngang màn hình để dễ chạm ngón cái. Thiết kế hiện tại hiển thị ở góc dưới phải.

Chỉnh sửa FloatingCTA hiện tại (sửa UI):
```tsx
'use client'

import { Phone } from 'lucide-react'

export function FloatingCTA() {
  return (
    <div className="fixed bottom-4 right-4 md:bottom-8 md:right-8 flex flex-col gap-4 z-50 pointer-events-none">
      {/* Zalo Button */}
      <a
        href="https://zalo.me/0905000000"
        target="_blank"
        rel="noopener noreferrer"
        className="pointer-events-auto h-[50px] md:h-[60px] w-auto px-4 md:px-0 md:w-[60px] bg-[#0068FF] text-white rounded-full 
                 shadow-lg flex items-center justify-center gap-2 hover:bg-[#005AE0] 
                 transition-colors relative lg:w-[60px] hover:w-auto overflow-hidden group 
                 animate-[bounce-small_2s_ease-in-out_infinite]"
        aria-label="Chat Zalo"
      >
        <span className="font-bold text-xl md:text-2xl bg-white text-[#0068FF] w-7 h-7 md:w-8 md:h-8 rounded-full flex items-center justify-center -ml-1 md:ml-0 shrink-0">Z</span>
        {/* Expanded text on hover or mobile */}
        <span className="font-bold whitespace-nowrap overflow-hidden transition-all duration-300 md:max-w-0 md:group-hover:max-w-[200px] md:group-hover:pl-2 md:group-hover:pr-2">
          Chat Zalo
        </span>
      </a>

      {/* Phone Button */}
      <div className="pointer-events-auto relative flex items-center">
        {/* Pulse effect */}
        <div className="absolute inset-0 bg-[#2D8B3A] rounded-full animate-ping opacity-60"></div>
        <div className="absolute inset-[-10px] bg-[#2D8B3A]/30 rounded-full animate-[ping_1.5s_cubic-bezier(0,0,0.2,1)_infinite]"></div>
        
        <a
          href="tel:0905000000"
          className="h-[50px] md:h-[60px] flex items-center bg-[#2D8B3A] border-2 border-white text-white rounded-full 
                   shadow-xl hover:bg-[#1E6B2A] transition-colors relative z-10 px-4 md:px-6"
          aria-label="Gọi ngay"
        >
          <div className="bg-white/20 p-2 rounded-full mr-3 shrink-0">
            <Phone size={18} className="fill-white md:w-5 md:h-5" />
          </div>
          <span className="font-exbold text-[15px] md:text-lg tracking-wide whitespace-nowrap">
            0905.000.000
          </span>
        </a>
      </div>
    </div>
  )
}
```

2. Update `Header.tsx`: Chuyển `#` thành `tel:0905000000`. Cho chữ "Gọi Ngay" trên mobile rõ hơn.
</action>

<acceptance_criteria>
- [ ] `FloatingCTA` shows expanded Zalo icon + number CTA side by side, perfectly scaled for thumb touch on 375px.
- [ ] Pulse animation effect is retained for Phone CTA.
- [ ] Links are `tel:` and `https://zalo.me/...`
</acceptance_criteria>

### Task 2: Fix Hero image and placeholders

<read_first>
- `src/sections/Hero.tsx`
</read_first>

<action>
Sửa Placeholder text trong `Hero.tsx` thành content hoàn chỉnh:
- "Ảnh Cleaning (WIP)" -> Dùng thẻ `div` với gradient layer màu xanh và text "Hơn 5 năm đồng hành cùng sự sạch sẽ" hoặc gắn ảnh background url: `https://images.unsplash.com/photo-1527515637462-cff94eecc1ac?q=80&w=2574&auto=format&fit=crop`
- Cập nhật số ĐT tương tự `0905.000.000` (CTA Button `tel:`) và Email trong Form
</action>

<acceptance_criteria>
- [ ] Hero UI mockup block contains a dummy realistic backdrop instead of basic gray div
- [ ] Desktop / Mobile views render cleanly around the Hero image block
</acceptance_criteria>
